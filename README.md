# EMAIL_SMTP_PROJECT 📧

A simple Python project to send emails automatically using **SMTP**.  
This project uses environment variables to keep email credentials secure and sends emails to multiple recipients.

---

## 📌 Project Description

This project demonstrates how to send emails using Python’s built-in `smtplib` library.  
Email credentials are stored safely using a `.env` file with the help of `python-dotenv`.

It is useful for:
- Sending notifications
- Alerts
- Bulk emails
- Learning SMTP concepts in Python

---

## 🛠️ Technologies Used

- Python
- SMTP (`smtplib`)
- `python-dotenv`
- Gmail / Any SMTP-supported email service

---

## 📂 Project Structure

EMAIL_SMTP_PROJECT/
│
├── app.py # Main Python file to send emails
├── requirements.txt # Required Python packages
├── .env # Environment variables (not uploaded to GitHub)
└── README.md # Project documentation


---


